function pulseConStrToNum(input) {
    let name = (input[0]);
    let lastname = (input[0]);
    let age = Number(input[0]);
    let city = (input[0]);
    console.log(`You are ${name} ${lastname}, a ${age}-years old person from ${city}.`);
}
pulseConStrToNum(['Niki', 'Nikolaev', 20, 'Sofia']);